import React from "react";
import { Card, CardBody, CardTitle, CardText } from "reactstrap";

const AuthorCard = () => (
  <Card body inverse color="secondary">
    <CardBody>
      <CardTitle>Title</CardTitle>
      <CardText>ho</CardText>
    </CardBody>
  </Card>
);

export default AuthorCard;
